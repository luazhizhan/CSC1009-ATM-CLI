package src.main.java;



public class CardReader {
    
    // Format transaction data with misc? to be printed here
    public boolean ReadCard(Card card) {
        
        // if(bank database doesnt contain card.getCardNumber())

        return true;
    }
}
